.DS_Store
node_modules/
dist/

# Sản phẩm build, sinh lại bằng `npm run build`. Commit nó vào git là nguồn của
# một lớp lỗi: content/ thay đổi mà bản đã commit vẫn được phục vụ khi build
# hỏng, nên giao diện hiện những bài không còn tồn tại.
generated/

# Cùng lý do: robots.txt và sitemap.xml trong public/ do build/build.js sinh ra.
public/robots.txt
public/sitemap.xml
